export default function Profile(){
  return(
    <main className='page card'>
      <h2>Profile / Settings</h2>
      <p>Display name, skill goal, main focus, level, password recovery reminders.</p>
    </main>
  );
}
